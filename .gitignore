# Next.js
/.next/
/out/
next-env.d.ts

# Fumadocs MDX generated source
/.source/

# Generated at build time by scripts/emit-search-index.mjs (see build script)
/public/search-index.json

# Synced content (content/{platform,sdk,cli}, pulled from source repos by
# sync-docs.yml) is committed, NOT ignored: `volcano cloud frontends deploy`
# packages the archive from .gitignore, so a tracked-but-ignored tree would be
# dropped from the deploy. Internal docs are excluded at the source (docs/public
# only). Do not hand-edit synced content.

# deps
node_modules/

.DS_Store
