# content/platform, content/sdk, content/cli, content/ai are committed by the
# sync bot (see .github/workflows/sync-docs.yml) — do not hand-edit them.

# Build output (whichever generator ends up serving this)
/public/
/build/
/dist/
/.hugo_build.lock

node_modules/
package-lock.json
.DS_Store
