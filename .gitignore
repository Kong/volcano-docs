# Next.js
/.next/
/out/
next-env.d.ts

# Fumadocs MDX generated source
/.source/

# Synced content (pulled from internal source repos by sync-docs.yml).
# Ignored so a stray/local sync can't publish internal docs; the sync workflow
# force-adds these deliberately (git add content). Do not hand-edit.
/content/platform/
/content/sdk/
/content/cli/

# deps
node_modules/

.DS_Store
